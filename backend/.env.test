## Local development environment (safe defaults)

# Postgres connection for Prisma (adjust user/pass/host/db if you run Docker or local Postgres)
DATABASE_URL=postgresql://postgres:password@localhost:5432/saas_platform?schema=public

# Server and frontend defaults
PORT=4000
FRONTEND_URL=http://localhost:3000
NODE_ENV=development

# Auth and email (placeholders)
JWT_SECRET=changeme-dev-jwt
SMTP_USER=your.email@example.com
SMTP_PASS=your-app-password

# Frontend public API URL expected by the frontend
NEXT_PUBLIC_API_URL=http://localhost:4000

# Ethereum public RPC (optional for local)
ETH_PROVIDER_URL=https://cloudflare-eth.com

# Optional: Prisma verbose logging
# DEBUG=prisma:*
# PRISMA_LOG_LEVEL=query


